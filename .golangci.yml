version: "2"

linters:
  exclusions:
    rules:
      # Unchecked Close() on defer/cleanup is idiomatic and intentional here.
      - linters:
          - errcheck
        source: '\.Close\(\)'
      # Test files may ignore errors on best-effort cleanup and helpers.
      - path: _test\.go
        linters:
          - errcheck
