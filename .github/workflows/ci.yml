name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Install Linux systray build dependency
        run: sudo apt-get update -q && sudo apt-get install -y libayatana-appindicator3-dev

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-ed:"; echo "$unformatted"; exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: Tests (race) + coverage gate
        run: |
          go test -race ./...
          bash scripts/coverage-gate.sh

      # Windows: pure-Go systray (Win32 syscalls), CGO_ENABLED=0 works.
      # Linux: tested natively above. macOS: separate job below (needs CGO + Cocoa).
      - name: Cross-compile check (windows)
        run: |
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o /dev/null .

  # macOS build check: fyne.io/systray uses CGO on Darwin; must run on macOS.
  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
      - name: Build darwin/arm64
        run: GOARCH=arm64 go build -o /dev/null .
      - name: Build darwin/amd64
        run: GOARCH=amd64 go build -o /dev/null .

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: Install Linux systray build dependency
        run: sudo apt-get update -q && sudo apt-get install -y libayatana-appindicator3-dev
      - uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  commit-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Denylist guard
        env:
          TALLYWELL_DENYLIST: ${{ secrets.TALLYWELL_DENYLIST }}
        run: |
          if [ -z "$TALLYWELL_DENYLIST" ]; then
            echo "No denylist configured; skipping."; exit 0
          fi
          bash scripts/check-denylist.sh --all

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Linux systray build dependency
        run: sudo apt-get update -q && sudo apt-get install -y libayatana-appindicator3-dev
      - name: Build binary
        run: go build -o tallywell .
      - name: Install Playwright
        working-directory: e2e
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: Run E2E smoke
        working-directory: e2e
        env:
          TALLYWELL_BIN: ${{ github.workspace }}/tallywell
        run: npx playwright test
